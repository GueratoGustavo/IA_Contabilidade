apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: financeiro
spec:
  selector:
    app: app-financeiro
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
